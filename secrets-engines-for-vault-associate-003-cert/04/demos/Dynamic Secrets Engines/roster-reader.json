{
    "db_name": "rosterdb",
    "creation_statements": [
      "CREATE ROLE \"{{name}}\" WITH LOGIN PASSWORD '{{password}}' VALID UNTIL '{{expiration}}'",
      "GRANT USAGE ON SCHEMA roster TO \"{{name}}\"",
      "GRANT SELECT ON ALL TABLES IN SCHEMA roster TO \"{{name}}\""
    ],
    "default_ttl": "24h",
    "max_ttl": "48h"
  }
  